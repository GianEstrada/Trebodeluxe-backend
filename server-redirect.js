// Treboluxe Backend - Auto-redirect to simple server
// Este archivo evita el error path-to-regexp usando simple-server.js

console.log('🚀 Starting Treboluxe Backend...');
console.log('🔄 Using simple-server.js to avoid path-to-regexp errors');

// Importar directamente el servidor simple
require('./simple-server.js');
